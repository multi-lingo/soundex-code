{
  "name": "soundex-code-www",
  "private": true,
  "dependencies": {
    "soundex-code": "^1.0.0"
  },
  "devDependencies": {
    "browserify": "^16.0.0",
    "tinyify": "^2.4.3"
  },
  "scripts": {
    "build": "browserify . -p tinyify -o build.js"
  }
}
